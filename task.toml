artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["generated_output_artifact"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse a small Apache-style access log and emit an exact JSON summary report."
solution_explanation = "Read /app/access.log, count total non-empty requests, count unique client IP addresses, find the most frequently requested path with first-seen tie breaking, and write the result to /app/report.json."
verification_explanation = "Parse /app/report.json and verify that it is a JSON object with exactly total_requests, unique_ips, and top_path matching the provided access log."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = false
mcp_servers = []
